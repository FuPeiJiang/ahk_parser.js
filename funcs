
commandFirstParamToFunction() :
  WinGetTitle, Title, WinTitle, WinText, ExcludeTitle, ExcludeText
  <- move left
  Title := WinGetTitle([WinTitle, WinText, ExcludeTitle, ExcludeText])


function findNext(stopAtThis: string)
if (next.type === stopAtThis) {

list of functions:
keepAsItIs(): v1 %v% will stay %v%
modV1StrToEdit(): v1Str won't be quoted
